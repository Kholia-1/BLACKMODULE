services:
  db:
    image: postgres:16
    container_name: blackmodule_postgres
    restart: always
    environment:
      POSTGRES_DB: blackmodule
      POSTGRES_USER: blackmodule_user
      POSTGRES_PASSWORD: blackmodule_password
    volumes:
      - blackmodule_postgres_data:/var/lib/postgresql/data
    ports:
      - "5433:5432"

  app:
    build: .
    container_name: blackmodule_app
    restart: always
    depends_on:
      - db
    environment:
      DATABASE_URL: postgresql://blackmodule_user:blackmodule_password@db:5432/blackmodule
      SECRET_KEY: CHANGE_ME_SECRET_KEY
      BLACKMODULE_API_KEY: CHANGE_ME_API_KEY
      INITIAL_ADMIN_PASSWORD: ${INITIAL_ADMIN_PASSWORD}
      LIST_MAX_ENTRY_DROP_PERCENT: ${LIST_MAX_ENTRY_DROP_PERCENT:-30}
      ALERT_SLA_EXACT_HOURS: ${ALERT_SLA_EXACT_HOURS:-4}
      ALERT_SLA_PROBABLE_HOURS: ${ALERT_SLA_PROBABLE_HOURS:-24}
      ALERT_SLA_POSSIBLE_HOURS: ${ALERT_SLA_POSSIBLE_HOURS:-72}
      ALERT_SLA_NEAR_RATIO: ${ALERT_SLA_NEAR_RATIO:-0.8}
      ALERT_INACTIVITY_HOURS: ${ALERT_INACTIVITY_HOURS:-24}
    ports:
      - "10000:10000"

volumes:
  blackmodule_postgres_data:
